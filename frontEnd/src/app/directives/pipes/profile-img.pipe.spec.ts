import { ProfileImgPipe } from './profile-img.pipe';

describe('ProfileImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileImgPipe();
    expect(pipe).toBeTruthy();
  });
});
